import { LoadingScreen } from "@/components/loading-screen";

export default function Loading() {
  return (
    <LoadingScreen
      title="Abrindo o dashboard"
      description="Estamos carregando leads, permissões e a visão geral da operação."
    />
  );
}
