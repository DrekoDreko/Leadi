import { LoadingScreen } from "@/components/loading-screen";

export default function Loading() {
  return (
    <LoadingScreen
      title="Abrindo a área da equipe"
      description="Carregando dados do workspace, membros e configurações de gestão."
    />
  );
}
